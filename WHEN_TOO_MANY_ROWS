SET SERVEROUTPUT ON;
DECLARE
X EMP.ENAME%TYPE;
BEGIN
  SELECT ENAME INTO X
  FROM EMP
  WHERE JOB1='MANAGER' ;
  DBMS_OUTPUT.PUT_LINE('HELLO');
EXCEPTION
  WHEN TOO_MANY_ROWS THEN
    DBMS_OUTPUT.PUT_LINE('NO ROWS WHERE REURNED');
END;
