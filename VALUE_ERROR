SET SERVEROUTPUT ON;
DECLARE
X NUMBER;
BEGIN
  SELECT ENAME INTO X
  FROM EMP
  WHERE EMPNO=7369 ;
EXCEPTION
  WHEN VALUE_ERROR THEN
    DBMS_OUTPUT.PUT_LINE('INCOMPATIBLE DATA TYPES OF X AND EMP NO');
END;
